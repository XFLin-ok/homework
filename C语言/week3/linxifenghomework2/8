
错误的有：
1.a  apple[2]为常量值，赋值表达式左边不能是常量
1.c  weight为浮点型常量而apple为数组名即改数组的首地址，将常量赋值给地址是错误的
2.e  apple_tree[4][4]为该数组元素的值，apple[3]也为该数组的值，不能把常量赋值给常量；
3.f  apple_tree[5]为地址常量，赋值表达式左边不能是常量
4.g  pf为一个指针变量，只能储存地址，而weight为一个浮点型常量不能赋值给指针变量；
